# RHCSA Practice Labs

## Production Notes

### Before merging to main/prod:
- Remove `/api/mock-stats` endpoint from `api/app.py` (dev-only testing endpoint)

## Commit Messages
- Do not include Claude Code attribution in commit messages
